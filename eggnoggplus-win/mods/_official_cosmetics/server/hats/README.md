# Hat Source Files

Put one `32x32` PNG per hat in this folder.

Each PNG can have a matching JSON file with the same basename. Example:

- `cap.png`
- `cap.json`

Run `..\build_cosmetics.ps1` from the `server` folder to rebuild `manifest.json` and `assets\hats.png`.
