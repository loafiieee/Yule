# Eggnogg+ Online - connection settings
# `input_resend_ticks` is a legacy setting from the snapshot path and is
# currently unused by the delayed-lockstep implementation.

server_host: str, "127.0.0.1"
server_port: int[1,65535], 7878
input_resend_ticks: int[1,10], 1

verbose_logging: bool, true
