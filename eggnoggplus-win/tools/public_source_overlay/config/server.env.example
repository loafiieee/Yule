# Install as a root-owned file such as /etc/eggnogg/lfg.env.
HOST=0.0.0.0
PORT=47778
UDP_HOST=0.0.0.0
UDP_PORT=47778
ADMIN_HOST=127.0.0.1
ADMIN_PORT=47779

# Set these only in the deployment environment, never in Git.
DISCORD_TOKEN=replace-me
DISCORD_CHANNEL_ID=replace-me
